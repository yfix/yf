language: php

build_environment: ubuntu1404

php:
  - 5.4
  - 5.5
  - 5.6
  - hhvm

services:
  - memcached

script: export YF_DB_PSWD=""; cd ./.dev/tests/bin/ && ./phpunit_run_all.sh

matrix:
  fast_finish: true
