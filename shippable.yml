language: php

build_environment: ubuntu1404

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - hhvm

services:
  - memcached
  - redis

script: export YF_DB_PSWD=""; php -v; php -m; cd ./.dev/tests/bin/ && ./phpunit_run_all.sh

matrix:
  fast_finish: true
