<h1>{header_text}</h1>

<form action="{form_action}" method="post">
<table class="table table-bordered table-striped table-hover">
	<thead>
		<th>{t(table_name)}<br /><br /></th>
		<th>{t(allowed)}</th>
		<th>{t(denied)}</th>
		<th>{t(for_debug)}</th>
		<th>{t(hide)}</th>
	</thead>
	<tbody>
{items}
	</tbody>
</table>
	<div align="center"><br><input type="submit" value="{t(save)}"></div>
</form>

