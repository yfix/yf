{if("items" ne "")}
<table align="center">
	<thead>
		<th>{t(Name)}</th>
		<th>{t(title)}</th>
		<th width="11%" align="center">{t(Action)}</th>
	</thead>
{items}
</table>
{/if}

{if("items" eq "")}<div align="center">{t(No communities yet)}</div>{/if}
<br />
