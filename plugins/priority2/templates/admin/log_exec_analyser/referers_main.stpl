<h2>{t(Top Referers)} ({t(total)} {total} {t(entries)})</h2>

<div align="center">
<a href="{main_link}">{t(Back to analyser main)}</a>
<br /><br />

<table class="table table-bordered table-striped table-hover">
	<thead>
		<th>{t(Hits)}</th>
		<th>{t(Referer)}</th>
	</thead>
	<tbody>
{items}
	</tbody>
</table>
</div>
{if("pages" ne "")}
<br /><br />
	<div align="center">{pages}</div>
{/if}
