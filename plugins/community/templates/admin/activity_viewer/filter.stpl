<div class="filter">
<legend>{t(Filter)}</legend>
<form action='{save_action}' method='post'>
	{form_row("text","user_id","User ID")}
	{form_row("box","task_id_box","Action")}
	{form_row("box","refresh_timeout_box","Refresh every")}
	{form_row("box","sort_by_box","Sort by field")}
	{form_row("box","sort_order_box","Sort order")}
	{form_row("save_and_clear")}
</form>
</div>