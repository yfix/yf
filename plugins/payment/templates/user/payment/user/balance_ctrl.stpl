{asset()} ng-balance-recharge {/asset}

{js()}
(function () { 'use strict';

var __NS__ = 'payment.balance.recharge';

angular.module( __NS__ )

.constant( 'Config', { payment: {payment} } )

;
})();
{/js}
