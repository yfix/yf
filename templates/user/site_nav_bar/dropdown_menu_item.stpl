<li class="dropdown">
	<a href="{link}" class="dropdown-toggle">{if("icon_path" ne "")}<img src="{icon_path}"> {/if}{if("icon_class" ne "")}<i class="{icon_class}"></i> {/if}
		{name} {if("have_children" eq "1")} <b class="caret"></b>{/if}
	</a>
{if("have_children" eq "1")}
	<ul class="dropdown-menu sub-menu">
{else}
	</li>
	{if("next_level_diff" ne "0")}
		{foreach("next_level_diff")}</ul>{/foreach}
	{/if}
{/if}
