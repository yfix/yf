{if("pages" ne "")}
<div class="pagination">
    <ul >
        {page_first}
        {block_prev}
    </ul>
    <ul >
        {page_prev}
        {pages}
        {page_next}
    </ul>
    <ul >
        {block_next}
        {page_last}
    </ul>
</div>
{/if}
