{if("form_header" ne "")}<h1>{form_header}</h1>{/if}
<div class="editform">
<form action="{form_action}" id="{form_id}" name="{form_name}" method="{form_method}" enctype="{form_enctype}">
{form_items}
</form>
</div>
{form_footer}