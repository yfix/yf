<h2>{t(Uninstall module)} "{module_name}"</h2>

{if("files_to_delete" ne "")}
<form action="{form_action}" method="post">
<table align="center">
	<thead>
		<th>#</th>
		<th>{t(Path)}</th>
	</thead>
	<tbody>
{files_to_delete}
	</tbody>
</table>
</form>
<br />
<div align="center">
	<input type="submit" value="{t(CONFIRM)}" name="go" onclick="return confirm('{t(Are you sure?)}')">
	<input type="button" value="{t(CANCEL)}" onclick="window.location.href='{back_link}'">
</div>
{/if}
