<form action="{save_form_action}" method="post" name="menu_form">

<ul class="draggable_menu">
{items}
</ul>

<div class="controls">
	<input type="submit" class="btn btn-primary" value="{t(Save)}">
	<a href="{add_item_link}" class="btn">{t(Add)}</a>
	<a href="{back_link}" class="btn">{t(Back)}</a>
</div>

</form>

{include("menus_editor/menu_items2_js")}
