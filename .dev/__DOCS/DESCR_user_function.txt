yf_user_data.class.php

	ћожет использоватьс€ в 2-х режимах:
		- режим единой таблицы дл€ инфо пользовател€ (var $MODE = "SIMPLE") - при этом функци€ будет 
	использовать таблицу <prefix>_user
		- режим динамической таблицы "info" (var $MODE = "DYNAMIC") - функци€ будет использовать таблицы 
	<prefix>_user_data_... и таблицы <prefix>_user_data_info_fields и <prefix>_user_data_info_values
	

function _user

	ѕредназначенa дл€ выборки информации из базы данных по заданным параметрам

	ѕередаваемые параметры:
		- $user_id 	(type: "int" or "array")
		- $fields	(type: "string" or "array")
		- $params	(type: "array")

	ќсобенности:
		$user_id может задаватьс€ как int (выборка информации дл€ одного пользовател€), 
	так и как array (дл€ нескольких). ¬ зависимости от этого возвращаемый результат - одномерный или 
	двумерный массивы.
		$fields может задаватьс€:
		 ключевыми словами: 
			"full" - выборка информации из всех полей
			"short" - кратка€ информаци€ о пользователе ("name", "nick", "group", "city", "country", "state")
			"main", "stats", "ban", "info" - выборка информации из таблиц c соответствующими названи€ми
		перечислением:
			"main,stats,info"
		≈сли таблицы с заданным именем не существует выборка из нее не производитс€.
		$params представл€ет собой массив ключами которого €вл€ютс€ служебные слова mysql (WHERE, ORDER BY, 
	GROUP BY, ...) a значени€ми - массивы "поле"=>"значение"
		Ќапример: массив array("WHERE"=>array("group"=>2, "active"=>1, "city" => "LIKE 'Mosco%'"))заданный в 
		качестве $params означает что выборка будет производитьс€ с дополнительным условием  
		WHERE `group`='2' AND `active`='1' AND `city` LIKE 'Mosco%'

	¬озвращаемый результат:
		- одномерный массив если (int) $user_id
		- двумерный массив c ключом "user_id" из массива $user_id если (array) $user_id
		- false - не существует всех указанных таблиц или полей

function _update_user

	ѕредназначена дл€ обновлени€ информации о пользователе

	ѕередаваемые параметры:
		- $user_id 	(type: "int")
		- $fields	(type: "array" "field"=>"value")
		- $params	(type: "string")

	¬озвращаемый результат:
	- true - при успешном выполнении
	- false - при ошибке
