+- 1. Файлы во фреймворке нельзя переписывать при импорте ни в коем случае.
+ 2. Если сайт совпадает с проектом (сайт в проекте один и REAL_PATH == INCLUDE_PATH)	 - то тогда сайт не показываем.
+ 3. "Themes library dir used" нужно всегда приводить к абсолютному виду (через realpath() ).
4. После запихивания в базу нужно добавить кнопку "Назад", то же и после вытаскивания из базы.
5. При показе списка шаблонов в теме хотелось бы еще один столбец, где указывается где шаблон есть (в файле, в базе).
