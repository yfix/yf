<h3 id="count"><a href="#count">count</a> {github(db/db_query_builder_driver.count)}</h3>
<p>
	<code>count</code> function counting number of records inside requested recordset.
	Default parameter is: <code>$id = '*', $as_sql = false</code>
	If parameter <code>$as_sql = true</code> return simple sql command,
	else - return query result
	* Example:
</p>

<h3 id="max"><a href="#max">max</a> {github(db/db_query_builder_driver.max)}</h3>
<p>
	<code>max</code> SQL aggregate MAX()
	Default parameter is: <code>$pk = null, $as_sql = false</code>
	If parameter <code>$as_sql = true</code> return simple sql command,
	else - return query result
	* Example:
</p>

<h3 id="min"><a href="#min">min</a> {github(db/db_query_builder_driver.min)}</h3>
<p>
	<code>min</code> SQL aggregate MIN()
	Default parameter is: <code>$pk = null, $as_sql = false</code>
	If parameter <code>$as_sql = true</code> return simple sql command,
	else - return query result
	* Example:
</p>

<h3 id="avg"><a href="#avg">avg</a> {github(db/db_query_builder_driver.avg)}</h3>
<p>
	<code>avg</code> SQL aggregate AVG()
	Default parameter is: <code>$pk = null, $as_sql = false</code>
	If parameter <code>$as_sql = true</code> return simple sql command,
	else - return query result
	* Example:
</p>

<h3 id="sum"><a href="#sum">sum</a> {github(db/db_query_builder_driver.sum)}</h3>
<p>
	<code>sum</code> SQL aggregate SUM()
	Default parameter is: <code>$pk = null, $as_sql = false</code>
	If parameter <code>$as_sql = true</code> return simple sql command,
	else - return query result
	* Example:
</p>

<h3 id="first"><a href="#first">first</a> {github(db/db_query_builder_driver.first)}</h3>
<p>
	<code>first</code> Return first item from resultset
	Default parameter is: <code>$select = null, $use_cache = false</code>
	If parameter <code>$use_cache = true</code> return cached query result
	* Example:
</p>

<h3 id="last"><a href="#last">last</a> {github(db/db_query_builder_driver.last)}</h3>
<p>
	<code>last</code> Return last item from resultset
	Default parameter is: <code>$select = null, $use_cache = false</code>
	If parameter <code>$use_cache = true</code> return cached query result
	* Example:
</p>
